class Quadruple: #key = name of the var, value = instance of Vars class
    
    def __init__(self, operation, left_op, right_op, tResult):
        self.operation = operation
        self.left_op = left_op
        self.right_op = right_op
        self.tResult = tResult