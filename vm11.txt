program factorialRecursivo :
    main {

        var int p;
        
        funct int testReturn(int val1) {
            var int a, g;


            if (val1 >= 1) then {
                g = val1 - 1;
                a = val1 * testReturn(g);
            }

            else {
                a = 1;
            }

            return a;
        }
        
        init {
            var int h, i, j, k;
                float harry, potter;

            p = 6;
            i = 5;
            k = i * testReturn(p);
            write(k);
        }
    }
end