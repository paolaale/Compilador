program p3 :
    
    class Perro {
        var int a, b, c;

        funct void getTotal(int a, int b){
            var float k;
            write("hola");
        }

    }
   
    main {

        var int p, h, j, k;

        funct void getTotal() {
            var int rubs, paola, alan;
                float omar, ale, edgar;
                char joe;

            write("pepepe");
        }

        init {
            var int p, f, g;
                float harry, potter;
            call getTotal();
            p = 19;
            write("hola");

            while (p < 7) do {
                write("adios");
            }

            if (k < 7) then {
                write("adios");
            } 
            elif (f == 7) then {
                write("adios");
            }
            elif (g > 7) then {
                write("adios");
            }
            else {
                write("adios");
            }
        
        }
    }
   
end