program c1 :

    main {

        init {
           
           var int a,b,c,d;

            while (b > c) do {
                a = b + d;

                if (b > d) then {
                    d = b + d;
                }
            }
        }
    }
end