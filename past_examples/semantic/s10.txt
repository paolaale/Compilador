program s10 :

    main {

        init {
            var int a, b, c, d;
            char x;
            
            c = b / c;

            write("hola", a + b);
            write("adios");
            write(b * c);
            read(c, d);

            b = d * a;
        }
    }
end
